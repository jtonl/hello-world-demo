# hello-world-demo

Demo repository for the workflow board. Runner-launched Claude Code sessions run headless
(`--permission-mode acceptEdits`), which denies `gh` by default; the committed
`.claude/settings.json` allows `Bash(gh:*)` in this repository only.
